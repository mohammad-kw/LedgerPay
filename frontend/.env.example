# Optional: override the API base URL (e.g. when deployed, if the
# frontend and backend are NOT served from the same origin). In local
# development, leave this unset - vite.config.js's dev proxy forwards
# "/api/..." requests to http://localhost:8080 automatically.
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
